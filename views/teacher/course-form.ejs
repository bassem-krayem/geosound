<%- include('../partials/head') %>
<%- include('../partials/navbar') %>

<main id="main-content">
  <div class="container">
    <div class="form-page">

      <%- include('../partials/flash') %>

      <!-- مسار التنقل -->
      <ol class="breadcrumb" aria-label="مسار التنقل">
        <li><a href="/teacher/dashboard">لوحة التحكم</a></li>
        <li><%= isEdit ? 'تعديل الدورة' : 'دورة جديدة' %></li>
      </ol>

      <h1 class="mb-3"><%= isEdit ? 'تعديل الدورة' : 'إنشاء دورة جديدة' %></h1>

      <div class="card">
        <div class="card-body">
          <form
            action="<%= isEdit ? '/teacher/courses/' + course._id + '/edit' : '/teacher/courses/new' %>"
            method="POST"
            novalidate
            aria-label="<%= isEdit ? 'نموذج تعديل الدورة' : 'نموذج إنشاء الدورة' %>"
          >
            <%- include('../partials/csrf') %>
            <div class="form-group">
              <label class="form-label" for="title">عنوان الدورة <span aria-hidden="true" class="text-danger">*</span></label>
              <input
                class="form-control"
                type="text"
                id="title"
                name="title"
                required
                aria-required="true"
                value="<%= typeof course !== 'undefined' && course.title ? course.title : '' %>"
              />
            </div>

            <div class="form-group">
              <label class="form-label" for="description">الوصف</label>
              <textarea
                class="form-control"
                id="description"
                name="description"
                maxlength="500"
                aria-describedby="desc-hint"
              ><%= typeof course !== 'undefined' && course.description ? course.description : '' %></textarea>
              <span class="form-hint" id="desc-hint">
                <span class="char-count"></span>
              </span>
            </div>

            <div class="d-flex gap-2 flex-wrap">
              <button type="submit" class="btn btn-primary"><%= isEdit ? 'حفظ التغييرات' : 'إنشاء الدورة' %></button>
              <a href="/teacher/dashboard" class="btn btn-secondary">إلغاء</a>
            </div>
          </form>
        </div>
      </div>

    </div>
  </div>
</main>

<%- include('../partials/footer') %>
